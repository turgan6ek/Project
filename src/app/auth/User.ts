export class User {
  first_name : string;
  last_name : string;
  password : string;
  email: string;
  isAdmin: boolean;
  id : number;
  constructor(first_name, last_name, password, email, isAdmin, id) {
  }
}
